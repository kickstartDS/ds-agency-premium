import { DeepPartial } from "../helpers";
import { SearchModalProps } from "./SearchModalProps";

const defaults: DeepPartial<SearchModalProps> = {
  "headline": "Search",
  "closeAriaLabel": "close"
};

export default defaults;