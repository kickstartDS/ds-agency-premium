import { DeepPartial } from "../helpers";
import { ButtonProps } from "./ButtonProps";

const defaults: DeepPartial<ButtonProps> = {
  "variant": "secondary",
  "size": "medium",
  "disabled": false
};

export default defaults;