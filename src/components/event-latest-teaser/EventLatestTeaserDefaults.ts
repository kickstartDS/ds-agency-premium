import { DeepPartial } from "../helpers";
import { EventLatestTeaserProps } from "./EventLatestTeaserProps";

const defaults: DeepPartial<EventLatestTeaserProps> = {
  "calendar": {}
};

export default defaults;