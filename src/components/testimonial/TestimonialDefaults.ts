import { DeepPartial } from "../helpers";
import { TestimonialProps } from "./TestimonialProps";

const defaults: DeepPartial<TestimonialProps> = {
  "quoteSigns": "normal",
  "image": {}
};

export default defaults;