import { DeepPartial } from "../helpers";
import { NavFlyoutProps } from "./NavFlyoutProps";

const defaults: DeepPartial<NavFlyoutProps> = {
  "items": [],
  "inverted": false
};

export default defaults;