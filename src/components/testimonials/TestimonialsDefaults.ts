import { DeepPartial } from "../helpers";
import { TestimonialsProps } from "./TestimonialsProps";

const defaults: DeepPartial<TestimonialsProps> = {
  "layout": "slider",
  "quoteSigns": "normal",
  "testimonial": []
};

export default defaults;