import { DeepPartial } from "../helpers";
import { HeadlineProps } from "./HeadlineProps";

const defaults: DeepPartial<HeadlineProps> = {
  "switchOrder": false,
  "level": "h2",
  "style": "h2",
  "spaceAfter": "small"
};

export default defaults;