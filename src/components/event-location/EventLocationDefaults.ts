import { DeepPartial } from "../helpers";
import { EventLocationProps } from "./EventLocationProps";

const defaults: DeepPartial<EventLocationProps> = {
  "dates": [],
  "displayMode": "spacious",
  "links": []
};

export default defaults;