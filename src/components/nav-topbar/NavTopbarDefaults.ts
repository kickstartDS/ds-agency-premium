import { DeepPartial } from "../helpers";
import { NavTopbarProps } from "./NavTopbarProps";

const defaults: DeepPartial<NavTopbarProps> = {
  "items": [],
  "inverted": false
};

export default defaults;